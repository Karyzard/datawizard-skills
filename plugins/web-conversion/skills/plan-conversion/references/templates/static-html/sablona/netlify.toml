# ==========================================================================
# Netlify Configuration — Static HTML Site
# ==========================================================================

[build]
  # No build step needed — serve files directly
  publish = "."

# --------------------------------------------------------------------------
# Security Headers
# --------------------------------------------------------------------------

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# --------------------------------------------------------------------------
# Form Notifications
#
# Uncomment to receive email notifications when forms are submitted.
# You can also configure notifications in the Netlify dashboard:
# Site settings > Forms > Form notifications
# --------------------------------------------------------------------------

# [[plugins]]
#   package = "@netlify/plugin-emails"
#
#   [plugins.inputs]
#     from = "noreply@{{COMPANY_EMAIL}}"
#     to = "{{FORM_RECIPIENT}}"
#     subject = "Nova zprava z kontaktniho formulare"

# --------------------------------------------------------------------------
# Custom 404 Page
#
# Uncomment after creating a 404.html file.
# --------------------------------------------------------------------------

# [[redirects]]
#   from = "/*"
#   to = "/404.html"
#   status = 404
