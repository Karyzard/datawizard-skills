---
name: ship
description: Commit all current changes and push to GitHub. Use when user types /ship or wants to ship/publish/commit-and-push their work.
---

# /ship — Commit & push

Commit all current changes and push to GitHub:

1. Run `git status` to see all untracked and modified files
2. Run `git diff` to understand the staged and unstaged changes
3. Run `git log --oneline -5` to see recent commit style
4. Stage all changes with `git add -A`
5. Create a commit with a clear, conventional commit message summarizing the changes
6. Push to the current branch

End the commit message with:

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
